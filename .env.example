# Flutter client configuration only. Anything compiled into a mobile app can be
# extracted, so never place database passwords, service-account keys, or private
# API secrets here.
APP_ENV=development
API_BASE_URL=
